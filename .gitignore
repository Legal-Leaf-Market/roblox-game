# Rojo build artifacts are generated, never committed...
*.rbxl
*.rbxlx
*.rbxm
*.rbxmx
sourcemap.json

# ...EXCEPT the committed preview build, refreshed intentionally so the
# place can be downloaded from GitHub and opened in Studio directly.
!dist/ThreePaths.rbxlx

# Editor/OS noise
.DS_Store
